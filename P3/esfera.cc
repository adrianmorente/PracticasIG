#include "esfera.h"
#include "math.h"

Esfera::Esfera(){
  //Ayudandome de un dibujo hecho a mano, introduzco los vertices y las caras
  // en orden antihorario
  // vector<float> perfil_aux;
  // float angulo=M_PI/2, suma=2*M_PI/100, radio=1;
  // for(int i=0; i<50; i++){
  //   perfil_aux.push_back(-radio*cos(angulo), -radio*sin(angulo), 0);
  //   angulo = angulo+suma;
  // }

}
